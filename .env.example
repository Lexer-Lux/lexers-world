NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
FUZZ_SECRET=replace-with-a-long-random-secret
# Optional fuzz tuning (outsider coordinate privacy envelope)
# FUZZ_MIN_DISTANCE_KM=2
# FUZZ_MAX_DISTANCE_KM=8
# FUZZ_COORDINATE_DECIMALS=5
# Optional FX provider tuning for cost conversion hints in event detail
# FX_PROVIDER_URL=https://open.er-api.com/v6/latest/USD
# FX_CACHE_TTL_SECONDS=21600
# Optional public profile link used by the UI badge + empty-state CTA
# NEXT_PUBLIC_LEXER_TWITTER_URL=https://x.com/LexerLux
# Optional fallback allowlist (comma-separated lowercase Twitter handles)
# INSIDER_ALLOWLIST=lexer_lux,friend_handle
# Optional: gate insider preview access (dev fallback)
# INSIDER_PREVIEW_TOKEN=replace-with-a-random-token
